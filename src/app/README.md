# Routing layer

Keep this directory limited to Next.js route files, layouts, loading states,
and error boundaries. Public store routes live under `(store)` and protected
admin routes live under `(admin)/admin`. Feature business logic belongs in
`src/modules/`.
