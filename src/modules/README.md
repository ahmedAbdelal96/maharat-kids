# Feature modules

Each business capability owns its validation, types, server functions, service,
repository, queries, mutations, constants, and feature-specific components.
The app router should depend on module entry points rather than duplicating
domain logic.
