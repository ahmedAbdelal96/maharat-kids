# Shipping operations components

The admin shipping screens are client leaves for dialogs, selection, and
transition feedback. They receive database-backed snapshots from the server
routes and call shipping server actions for every mutation.
